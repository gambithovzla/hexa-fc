export async function resolvePendingPicks() {
  console.log('[pick-resolver] Soccer auto-resolve is pending implementation.');
  return [];
}

export async function resolveGamePicks(gamePk) {
  void gamePk;
  console.log('[pick-resolver] Soccer manual resolve is pending implementation.');
  return [];
}
